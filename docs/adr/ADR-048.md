# ADR-048: TTS-Streaming — erster Ton nach dem ersten Chunk

## Problem/Kontext

PO-Befund 10.07.2026: Sprach-Dialoge fühlen sich deutlich langsamer an als
ChatGPT-Voice. Das Messinstrument der Latenz-Scheibe 1 lieferte die Zahlen:
Vom Satzende bis zum ersten Ton vergehen ~5–11 s, und der größte
Einzelposten ist die **TTS-Synthese mit 2–5 s** (gemessen: 2,1 / 1,7 / 5,4 s
bei drei realen Zurufen). Ursache: Der bisherige Weg wartet, bis OpenAI die
**komplette** WAV-Datei geliefert hat, repariert den Header und spielt sie
dann per `winsound.PlaySound` ab — die gesamte Synthese-Zeit ist Wartezeit.

## Entscheidung

1. **`OpenAITTSBackend.stream_pcm(text)`**: fordert `response_format="pcm"`
   (rohes 24-kHz-/16-bit-/mono-PCM, kein Header, kein Reparatur-Thema) über
   die Streaming-API an und liefert die Chunks als Generator.
2. **`SpeechEngine._speak_streaming`**: spielt die Chunks sofort über
   `sounddevice.RawOutputStream` (Paket ist als Mikrofon-Dependency bereits
   an Bord) — der erste Ton kommt nach dem ersten Chunk statt nach der
   kompletten Synthese. Int16-Ausrichtung wird gepuffert, ein kurzer
   Stille-Schwanz (150 ms) schützt die letzte Silbe vor dem Stream-Ende.
3. **Datei-Weg bleibt der Rückfall** — fail-closed in beide Richtungen:
   Backend ohne `stream_pcm` (Piper/ElevenLabs/Kokoro), fehlendes
   sounddevice, `tts_streaming=false` oder JEDER Fehler **vor** dem ersten
   Ton ⇒ bewährter `synthesize_to_file`+`winsound`-Pfad. Bricht der Stream
   **mitten in** der Wiedergabe ab, wird bewusst NICHT wiederholt (kein
   doppeltes Anreden), sondern mit ERROR geloggt.
4. **Kill-Switch `tts_streaming`** (Config, Default `true`): ein Wert, kein
   Code — Rollback-Disziplin wie bei `answer_model`/`transcription_model`.
5. **Messbar**: Der Streaming-Pfad loggt „erster Ton nach X.Xs · gesamt
   Y.Ys" — dieselbe Messgröße wie vor der Scheibe, direkter A/B-Beweis.

## Begründung

- Die Messdaten zeigen: Synthese-Wartezeit ist der dickste Latenz-Brocken;
  Streaming eliminiert ihn fast vollständig (erwartet: erster Ton nach
  wenigen hundert ms statt 2–5 s).
- PCM statt WAV umgeht die kaputten Größenfelder (ADR-Historie: WAV-Header-
  Fix) vollständig — es gibt keinen Header mehr, der lügen könnte.
- `sounddevice` ist bereits Runtime-Dependency (Mikrofon/Wake-Word) — kein
  neues Paket, nur die Gegenrichtung desselben Werkzeugs.

## Alternativen

- **Satzweises Sprechen (Antwort-Stream → TTS je Satz):** größerer Umbau
  (Stufe 4 des Latenz-Fahrplans), lohnt erst, wenn die Verarbeitung selbst
  dominiert — tut sie nicht (1,2–1,8 s). Bleibt Backlog.
- **winsound mit Teil-WAVs:** stückweises Abspielen erzeugt hörbare Lücken
  an den Stückgrenzen — verworfen.
- **Streaming auch für Piper:** lokal ist die Synthese schnell genug;
  Komplexität ohne gemessenen Bedarf — verworfen (Falsifizierbarkeit unten).

## Konsequenzen

- Gesprochene Antworten beginnen spürbar früher; die Sprechdauer selbst
  bleibt unverändert (Wiedergabe in Echtzeit).
- Während des Streams ist der `_speaking`-Selbstschutz unverändert aktiv
  (gesetzt vom HotkeyChannel um den kompletten speak-Aufruf).
- Ein Abbruch mitten im Satz ist neu möglich (Netzabriss) — bewusst ohne
  Wiederholung, mit ERROR-Log.

## Falsifizierbarkeit

Widerlegt, wenn die geloggte „erster Ton nach"-Zeit nicht deutlich unter
den bisherigen Synthese-Zeiten (2–5 s) liegt oder Stream-Abbrüche im
Alltag häufiger als vereinzelt auftreten — dann `tts_streaming=false` und
Rückbau bzw. Stufe-4-Ansatz prüfen.

## Bewusst NICHT

- Kein Antwort-/LLM-Streaming (Stufe 4, eigene Entscheidung bei Bedarf).
- Kein Streaming für Wake-Bestätigungen (bleiben vorsynthetisiert im Cache
  — dort zählt Sofortigkeit aus dem Speicher, nicht Synthese-Latenz).
- Keine Lautstärke-/Geräteauswahl im Streaming-Pfad (wie bisher: System-
  Standardgerät).

## Status

Accepted (PO-Freigabe 10.07.2026, „go").
