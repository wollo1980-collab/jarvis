[pytest]
# Basetemp wird pro Lauf eindeutig in conftest.py::pytest_configure gesetzt
# (ausserhalb des Repos), damit pytest nie ein evtl. gesperrtes altes
# .pytest_tmp aufraeumen muss (Windows/Codex-Sandbox-Lock).
